## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] Tests pass locally (`pixi run test`)
- [ ] Lint & format clean (`pixi run ruff check .` and `pixi run ruff format --check .`)
- [ ] Pre-commit hooks pass (`pixi run pre-commit run --all-files`)
- [ ] Docs / README updated if behavior changed
- [ ] PR targets `dev` (not `main`), per the feature → dev → main flow
