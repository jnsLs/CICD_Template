[project]
authors = [{name = "Jonas", email = "jonas.lederer@tu-berlin.de"}]
dependencies = ["cowpy>=1.1.5,<2"]
name = "cicd_template"
requires-python = ">=3.14,<3.15"
version = "0.1.1"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project.scripts]
hello-world = "cicd_template.cli:main"

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
cicd_template = { path = ".", editable = true }

[tool.pixi.tasks]
start = "python ./src/cicd_template/cli.py"
test = "pytest --cov=src --cov-report=term-missing"

[tool.pixi.dependencies]
pre-commit = ">=4.6.0,<5"
ruff = ">=0.16.6,<0.17"
pytest = ">=9,<10"
pytest-cov = ">=7,<8"
python-build = ">=1,<2"

[tool.ruff]
line-length = 88
target-version = "py311"
preview = true

[tool.ruff.lint]
# E/W pycodestyle, F pyflakes, I import sorting, B bugbear, UP pyupgrade.
select = ["E", "F", "W", "I", "B", "UP"]
